<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:av="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cvt="clr-namespace:TileDownloader.Converters"
    mc:Ignorable="av" x:Class="TileDownloader.Views.MainWindow"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="Map Downloader" Height="600" Width="800"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="微软雅黑">
    <Window.Resources>
        <cvt:ExtentConverter x:Key="ExtentCvt" />
        <Style TargetType="{x:Type Label}">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Right" />
        </Style>
    </Window.Resources>
    <TabControl Margin="10">
        <TabItem Header="下载参数">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Content="下载来源" />
                    <ComboBox Grid.Column="1"  SelectedIndex="0" ItemsSource="{Binding Sources}" SelectedItem="{Binding Source}" DisplayMemberPath="Name" />
                </Grid>
                <Grid Grid.Row="1">

                    <ItemsControl ItemsSource="{Binding Arguments}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"></ColumnDefinition>
                                        <ColumnDefinition></ColumnDefinition>
                                    </Grid.ColumnDefinitions>
                                    <Label Content="{Binding Name}"></Label>
                                    <TextBox Text="{Binding Value}" Margin="5" VerticalContentAlignment="Center" IsReadOnly="{Binding IsReadOnly}" Grid.Column="1"></TextBox>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
                <StackPanel Grid.Row="2">
                    <Button Content="下载" Command="{Binding DownloadCmd}" Grid.Column="3" Margin="5" />
                </StackPanel>
            </Grid>
        </TabItem>
        <TabItem Header="下载任务">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="100" />
                </Grid.ColumnDefinitions>
                <DataGrid IsReadOnly="True" AutoGenerateColumns="False" Grid.ColumnSpan="3"  Margin="5" ItemsSource="{Binding Tasks}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="Auto" />
                        <DataGridTextColumn Header="总量" Binding="{Binding Total}" Width="Auto" />
                        <DataGridTextColumn Header="成功" Binding="{Binding Success}" Width="Auto" />
                        <DataGridTextColumn Header="失败" Binding="{Binding Fail}" Width="Auto" />
                        <DataGridTextColumn Header="速度" Binding="{Binding Speed,StringFormat=F0}" Width="Auto" />
                        <DataGridTemplateColumn Header="进度" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ProgressBar Minimum="0" Maximum="100" Value="{Binding Progress}"></ProgressBar>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="信息" Binding="{Binding Message}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>

                <ProgressBar Grid.Row="1" Margin="5" Value="{Binding Progress}" Height="20" Minimum="0" Maximum="100" />
                <Label Grid.Row="1"  Grid.Column="1" Content="{Binding Message}"></Label>
                <Button Grid.Row="1"  Grid.Column="2">取消</Button>
            </Grid>
        </TabItem>
    </TabControl>
</Window>