 <Window x:Class="TileDownloader.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="600" Width="800" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"     >
    <materialDesign:Card Margin="10" Padding="10">
        <Grid  >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Margin="5" Content="数据来源" Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <ComboBox Grid.Column="1" Margin="5"   ItemsSource="{Binding Sources}" SelectedItem="{Binding SelectedSource}" DisplayMemberPath="Name"></ComboBox>
            <Label Content="下载范围" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Grid Grid.Row="1" Grid.Column="1" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Label Grid.Column="0"  Content="MinX" Margin="5"></Label>
                <TextBox  Grid.Column="1" Margin="5" Text="{Binding MinX}"></TextBox>

                <Label Grid.Column="2"  Content="MinY" Margin="5"></Label>
                <TextBox  Grid.Column="3" Margin="5" Text="{Binding MinY}"></TextBox>

                <Label Grid.Column="4"  Content="MaxX" Margin="5"></Label>
                <TextBox  Grid.Column="5" Margin="5" Text="{Binding MaxX}"></TextBox>

                <Label Grid.Column="6"  Content="MaxY" Margin="5"></Label>
                <TextBox  Grid.Column="7" Margin="5"  Text="{Binding MaxY}"></TextBox>

                <Label Grid.Column="8"  Content="MaxLevel" Margin="5"></Label>
                <TextBox  Grid.Column="9" Margin="5"  Text="{Binding MaxLevel}" Cursor="IBeam"></TextBox>
            </Grid>
            <Label Content="请求参数" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <Grid Grid.Row="2" Grid.Column="1" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="35"/>
                    <RowDefinition Height="35"/>
                    <RowDefinition Height="35"/>
                </Grid.RowDefinitions>
                <Label Content="Url Formatter" HorizontalAlignment="Center" VerticalAlignment="Center"></Label>
                <TextBox Grid.Row="0" Grid.Column="1" Margin="5" Text="{Binding SelectedSource.UrlFormatter}"></TextBox>
                <Label Grid.Row="1" Content="Api Key" HorizontalAlignment="Center" VerticalAlignment="Center"></Label>
                <TextBox Grid.Row="1" Grid.Column="1" Margin="5" Text="{Binding SelectedSource.ApiKey}"  ></TextBox>
                <Label Grid.Row="2" Content="User Agent" HorizontalAlignment="Center" VerticalAlignment="Center"></Label>
                <TextBox Grid.Row="2" Grid.Column="1" Margin="5" Text="{Binding SelectedSource.UserAgent}"></TextBox>
            </Grid>
            <DataGrid IsReadOnly="True" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="5" ItemsSource="{Binding Status}">

            </DataGrid>
            <Label Grid.Row="4" Content="文件路径" HorizontalAlignment="Center" VerticalAlignment="Center"></Label>
            <Grid Grid.Row="4" Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Margin="5" Text="{Binding FilePath}" VerticalContentAlignment="Center" Cursor="IBeam"></TextBox>
                <Button Content="浏览" Grid.Column="1" Margin="5" Command="{Binding BrowseCmd}" Cursor="Hand"/>
            </Grid>
            <Grid Grid.Row="5" Grid.ColumnSpan="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ProgressBar Margin="5" Value="{Binding Progress}" Height="20" Minimum="0" Maximum="100"></ProgressBar>
                <Button Content="下载" Command="{Binding DownloadCmd}" Grid.Column="1" Margin="5" Cursor="Hand"/>
            </Grid>
        </Grid>
    </materialDesign:Card>
</Window>
