<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:av="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:c="clr-namespace:MapDownloader.Controls"
    xmlns:cvt="clr-namespace:MapDownloader.Converters"
    mc:Ignorable="av" x:Class="MapDownloader.Views.MainWindow"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Title="地图下载器" Height="600" Width="800"
    TextElement.Foreground="{DynamicResource MaterialDesignBody}"
    Background="{DynamicResource MaterialDesignPaper}"
    
    WindowState="Maximized"
    FontFamily="微软雅黑" Icon="/Resources/logo.png">
    <Window.Resources>
        <Style TargetType="{x:Type Label}">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Right" />
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
        </Style>
        <cvt:EnvelopeConverter x:Key="EnvelopeCvt"></cvt:EnvelopeConverter>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" MinHeight="100" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" MinHeight="100" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <GroupBox  Margin="5"   >
            <GroupBox.Header>
                <TextBlock Foreground="White" Text="下载参数"></TextBlock>
            </GroupBox.Header>
            <ScrollViewer>
                <StackPanel Margin="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="60" />
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Content="来源" />
                        <ComboBox Grid.Column="1" Margin="5"  SelectedIndex="0" ItemsSource="{Binding Sources}" SelectedItem="{Binding Source}" DisplayMemberPath="Name" />
                    </Grid>
                    <Grid  >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="60" />
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Content="范围" Margin="5" />
                        <TextBox  Margin="5" Grid.Column="1"  Text="{Binding Range,Converter={StaticResource EnvelopeCvt}}"/>
                        <md:PopupBox Grid.Column="2" Margin="5" Padding="0" StaysOpen="True"  >
                            <c:MapPickControl Height="600" Width="800"  Range="{Binding Range,Mode=TwoWay}" ></c:MapPickControl>
                        </md:PopupBox>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="60" />
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Content="并发" Margin="5" />
                        <TextBox  Margin="5" Grid.Column="1"  Text="{Binding Concurrent}"/>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="60" />
                        </Grid.ColumnDefinitions>
                        <Label Content="重试" Margin="5"  />
                        <TextBox  Margin="5" Grid.Column="1"  Text="{Binding Retry}"/>
                    </Grid>
                    <Grid  >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="60" />
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Content="路径" Margin="5" />
                        <TextBox  Margin="5" Grid.Column="1"  Text="{Binding Path}"/>
                        <Button Grid.Column="2" Margin="5" Cursor="Hand" Command="{Binding BrowseCmd}" Foreground="White">...</Button>
                    </Grid>
                    <ItemsControl ItemsSource="{Binding Arguments}" >
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"></ColumnDefinition>
                                        <ColumnDefinition Width="*"></ColumnDefinition>
                                        <ColumnDefinition Width="60" />
                                    </Grid.ColumnDefinitions>
                                    <Label Content="{Binding Name}"></Label>
                                    <TextBox Text="{Binding Value}" Margin="5" VerticalContentAlignment="Center" IsReadOnly="{Binding IsReadOnly}" Grid.Column="1" ></TextBox>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </GroupBox>
        <GridSplitter Grid.Row="1" HorizontalAlignment="Stretch" Height="5" Margin="5 0" Background="Transparent"/>
        <GroupBox Grid.Row="2" Margin="5">
            <GroupBox.Header>
                <TextBlock Foreground="White" Text="下载任务"></TextBlock>
            </GroupBox.Header>
            <DataGrid   IsReadOnly="True" AutoGenerateColumns="False" Grid.ColumnSpan="3"  ItemsSource="{Binding Tasks}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="Auto" />
                    <DataGridTextColumn Header="总量" Binding="{Binding Total}" Width="Auto" />
                    <DataGridTextColumn Header="完成" Binding="{Binding Completed}" Width="Auto" />
                    <DataGridTemplateColumn Header="进度" Width="240">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <ProgressBar Minimum="0" Maximum="100" Value="{Binding Progress}" Height="10" Margin="5"></ProgressBar>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="错误" Binding="{Binding Error}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="120" />
                <ColumnDefinition Width="120" />
            </Grid.ColumnDefinitions>
            <ProgressBar Margin="5" Height="Auto" Minimum="0" Maximum="100" Value="{Binding Progress}" ></ProgressBar>
            <Label Margin="5"  Content="{Binding Message}" Foreground="White" ></Label>
            <Button Content="下载" Command="{Binding DownloadCmd}" Grid.Column="1" Margin="5" Foreground="White" />
            <Button Content="取消" Command="{Binding CancelCmd}" Grid.Column="2" Margin="5" Foreground="White" Cursor="Hand" />
        </Grid>
    </Grid>
</Window>